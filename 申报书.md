# MoonHive 项目申报书

## 一、项目名称

MoonHive

## 二、项目简介

MoonHive 是 MoonBit 生态验证引擎：把候选 MoonBit 包拉入隔离工作区，用**真实的 MoonBit 工具链**验证它能否编译、测试是否通过，并把结果归类为九种可行动结论（✅ Verified / ⚠️ TestFailing / ❌ DoesNotCompile / 🚫 ToolchainMismatch / 📦 MissingDependency / 📄 NoManifest / ⏱️ TimedOut / 🔒 Unsafe / 🌐 FetchFailed）。

支持两种获取方式：`git clone` 验证仓库最新代码；`--registry` 模式用 `moon fetch` 验证注册表发布版本——即用户 `moon add` 真正会装到的那个版本。当前实现 `survey` / `inspect` / `doctor` / `serve` 四个命令，其中 `serve` 提供本地浏览器仪表盘，把验证结果可视化展示，**119 个单元测试全绿、构建零警告、零第三方依赖**。

## 三、为什么选择 MoonBit

报名时我尚不了解 MoonBit，正是"这个黑客松为什么叫 MoonBit"的好奇带我入门。开发后确认本项目与 MoonBit 高度契合：

1. **项目即生态**："用 MoonBit 验证 MoonBit"，服务生态本身的工具天然契合大赛主题；
2. **CLI 工具方向**：官方参考项目方向包含"经典命令行工具的现代替代"，生态验证引擎正属此类；
3. **编译为 WASM 后可跨平台分发**：体积小、启动快，适合命令行工具场景。

## 四、评审反馈与迭代改进

本项目三轮迭代，每轮均按评审意见重新定位：

1. 初版 Base64/Hex 编解码插件，被指"选题规模不足且与 Mooncakes 工具重叠"；
2. v1 转向"每日项目投喂"工具，因申报书与仓库更新不同步被驳回；
3. 现重构为 **生态验证引擎 v2.0**，选题与 Mooncakes 包仓库、GitHub 搜索形成明确差异——它们告知"项目存在"，MoonHive 告知"包在特定工具链上跑不跑得起来"。

## 五、原创性与开源合规

- 全部代码原创，经本人审阅、修改与调试；
- **自建 C FFI 平台层**：`moonbitlang/core` 不含文件系统与进程模块，自建 platform 层而非引入第三方包，依赖面为零；
- 诊断分类器（九类结论）为自研核心资产，特征串刻意保守——宁可冤枉一个包，不掩盖一个坏包；
- **安全设计**：隔离工作区 + 静态预检，含 FFI/构建脚本的仓库只做静态分析，永不执行仓库自带构建脚本；
- License：MIT。

## 六、预期目标

9/30 前完成文档与边界测试收尾，发布至 mooncakes.io；后续扩展多工具链对比与趋势追踪，沉淀为 MoonBit 生态基础设施工具。

## 七、GitHub 仓库链接

https://github.com/Duckweed-yhb/moon-hive
